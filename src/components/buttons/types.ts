/**
 * Button variant types - these are the source of truth for all button appearances
 * Used by both the Button components and Payload CMS link fields
 */
export type ButtonVariant = 'default' | 'secondary' | 'outline' | 'ghost' | 'link' | 'destructive'

export type ButtonSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl'
